const fs = require('fs');
const expect = require('chai').expect;
const login = require('./login');

/*describe('The login module', function () {
    describe('#renderLogin()', function () {
        it('routes to the correct function', function () {

            const req = this.sandbox.spy();
            const res = this.sandbox.spy();

            //login.renderLogin(req, res)

            //expect(res).to.be.called();

        })
    })
});
*/